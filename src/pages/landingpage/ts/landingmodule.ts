export type CartProps = {
    name: string,
    quantity: number,
    color: string,
    specs: string,
    picture: string
}

export type ColorVariantsProps = {
    value: string
    color: string

}